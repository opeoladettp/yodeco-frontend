# Development Environment Configuration
# This file is used when running the frontend locally (npm start)
# and connecting to the EC2 backend

# Backend API URL (EC2 instance)
REACT_APP_API_URL=http://localhost:5000/api

# Environment
NODE_ENV=development

# Enable development features
REACT_APP_DEBUG=true

# OAuth Configuration
# When testing OAuth locally, you can add ?redirect_to=localhost to the OAuth URL
# Example: https://yodeco-backend.duckdns.org/api/auth/google?redirect_to=localhost